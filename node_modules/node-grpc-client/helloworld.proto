syntax = "proto3";

package com.pkgtest;

service TestService {
    rpc sum(SumRequest) returns (SumResponse);
}

message SumRequest {
    repeated int32 nums = 1;
}

message SumResponse {
    int32 total = 1;
}